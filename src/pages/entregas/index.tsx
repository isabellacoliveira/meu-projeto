import { Box, Heading, StackDivider, Image, Text, Divider, Button, ButtonGroup } from "@chakra-ui/react"; // Certifique-se de importar o Text
import { Card, CardBody, CardFooter, Stack } from "react-bootstrap";

export default function Entregas() {
    return (
        <>
            <div className="container">
            </div>
        </>
    );
}
